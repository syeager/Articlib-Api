name: articlib-articles-ci

on:
  push:
    branches-ignore:
      - "private/**"
  pull_request:
    branches-ignore:
      - "private/**"

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v2
        with:
          submodules: recursive

      - name: Test
        if: ${{ false }}
        run: docker build ./Articlib -f ./Articlib/Articles/Source/Articlib.Articles.Api/Dockerfile --target test

      - name: Build
        run: docker build ./Articlib -f ./Articlib/Articles/Source/Articlib.Articles.Api/Dockerfile --tag articlib-articles:ci-$(date +%s)
